/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    images: {
        domains: ['www.capitalrush.gg','res.cloudinary.com'],
      },
}

module.exports = nextConfig
