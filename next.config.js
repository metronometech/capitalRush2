/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    images: {
        domains: ['www.capitalrush.gg','res.cloudinary.com', 'media.giphy.com'],
      },
}

module.exports = nextConfig
